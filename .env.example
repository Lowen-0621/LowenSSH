# 复制为 .env 后填写真实值；.env 已被 Git 忽略。
MYSQL_PASSWORD=请替换为MySQL强密码
GLM_API_KEY=请替换为智谱AI密钥

# 可用 openssl rand -base64 32 生成。部署后必须安全备份，丢失将无法解密已保存凭据。
XWSSH_CRYPTO_KEY=请替换为随机主密钥

# 轮换示例：
# XWSSH_CRYPTO_KEYS=v2=新密钥,v1=旧密钥
# XWSSH_ACTIVE_CRYPTO_KEY_VERSION=v2
